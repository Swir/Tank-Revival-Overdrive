name: Command Network Windows Gate

on:
  push:
    branches:
      - 'dev-v5-9'
    paths:
      - '.dev-build-trigger'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-command-candidate:
    name: Compile v5.9 Windows x64
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Verify Unity CI credentials
        shell: bash
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        run: |
          test -n "$UNITY_LICENSE"
          test -n "$UNITY_EMAIL"
          test -n "$UNITY_PASSWORD"
      - name: Cache Unity Library
        uses: actions/cache@v4
        with:
          path: Library
          key: CommandNet-Library-${{ runner.os }}-6000.3.17f1-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            CommandNet-Library-${{ runner.os }}-6000.3.17f1-
            Fortress-Library-${{ runner.os }}-6000.3.17f1-
            Dev-Library-${{ runner.os }}-6000.3.17f1-
      - name: Build development player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          projectPath: .
          unityVersion: 6000.3.17f1
          targetPlatform: StandaloneWindows64
          buildMethod: TankRevival.Editor.CIBuild.BuildWindows
          allowDirtyBuild: true
      - name: Verify executable
        shell: bash
        run: |
          test -f build/StandaloneWindows64/TankRevivalOverdrive.exe
          test -d build/StandaloneWindows64/TankRevivalOverdrive_Data
      - name: Pack exact candidate
        shell: bash
        run: |
          cd build/StandaloneWindows64
          zip -r ../../TankRevivalOverdrive-v5.9-command-network-Windows-x64.zip .
      - name: Upload exact candidate
        uses: actions/upload-artifact@v4
        with:
          name: TankRevivalOverdrive-v5.9-command-network-Windows-x64
          path: TankRevivalOverdrive-v5.9-command-network-Windows-x64.zip
          if-no-files-found: error
          retention-days: 14

  command-runtime-smoke:
    name: Boot packaged EXE and verify v5.9 command network
    needs: build-command-candidate
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Download exact candidate
        uses: actions/download-artifact@v4
        with:
          name: TankRevivalOverdrive-v5.9-command-network-Windows-x64
          path: candidate
      - name: Extract candidate
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Set-Location candidate
          Expand-Archive 'TankRevivalOverdrive-v5.9-command-network-Windows-x64.zip' -DestinationPath runtime -Force
          if (!(Test-Path 'runtime/TankRevivalOverdrive.exe')) { throw 'EXE missing after extraction' }
      - name: Run v5.9 command-network smoke
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Set-Location candidate/runtime
          Remove-Item COMMAND_NETWORK_PASS.txt, COMMAND_NETWORK_FAIL.txt, CommandNetworkPlayer.log -ErrorAction SilentlyContinue
          $p = Start-Process -FilePath '.\TankRevivalOverdrive.exe' -ArgumentList '-batchmode','-nographics','-command-network-smoke','-logFile','CommandNetworkPlayer.log' -PassThru
          if (!$p.WaitForExit(60000)) { Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue; throw 'Command-network smoke timed out after 60 seconds' }
          if (Test-Path 'COMMAND_NETWORK_FAIL.txt') { Get-Content COMMAND_NETWORK_FAIL.txt; if (Test-Path CommandNetworkPlayer.log) { Get-Content CommandNetworkPlayer.log -Tail 180 }; throw 'Command-network runtime probe reported FAIL' }
          if (!(Test-Path 'COMMAND_NETWORK_PASS.txt')) { if (Test-Path CommandNetworkPlayer.log) { Get-Content CommandNetworkPlayer.log -Tail 180 }; throw "Runtime exited without command-network PASS marker (exit=$($p.ExitCode))" }
          Get-Content COMMAND_NETWORK_PASS.txt
          if ($p.ExitCode -ne 0) { throw "Runtime returned non-zero exit code $($p.ExitCode)" }
          if (Test-Path CommandNetworkPlayer.log) {
            if (Select-String -Path CommandNetworkPlayer.log -Pattern 'Crash!!!|NullReferenceException|MissingMethodException|TypeLoadException|IndexOutOfRangeException|UnityException' -Quiet) { Get-Content CommandNetworkPlayer.log -Tail 220; throw 'Blocking exception signature found during v5.9 command-network runtime smoke' }
          }
          Write-Host 'v5.9 command-network runtime smoke passed.'
      - name: Upload command-network diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TankRevival-v5.9-command-network-diagnostics
          path: |
            candidate/runtime/COMMAND_NETWORK_PASS.txt
            candidate/runtime/COMMAND_NETWORK_FAIL.txt
            candidate/runtime/CommandNetworkPlayer.log
          if-no-files-found: ignore
          retention-days: 14
